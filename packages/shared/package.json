{
    "name": "@aws-app/shared",
    "version": "1.0.0",
    "description": "Shared types and utilities for AWS App monorepo",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "private": true,
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rm -rf dist"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}